import 'package:flutter/material.dart';
import 'package:socialize/config/app_config.dart';

// Placeholder for API keys - User should replace these
final String googleMapsApiKeyAndroid = AppConfig.googleApiKey ?? "FALLBACK_ANDROID_KEY";

// Theme related (though AppTheme will define more)
const Color primaryColorLight = Colors.blue; // Example
const Color primaryColorDark = Colors.lightBlueAccent; // Example