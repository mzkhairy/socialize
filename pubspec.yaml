name: socialize
description: A new Flutter project for finding and creating local activities.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.2 # Adjust based on your Flutter SDK version

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.0.5
  google_maps_flutter: ^2.12.2
  geolocator: ^11.0.0       # Check for the latest version
  geocoding: ^3.0.0         # Check for the latest version
  shared_preferences: ^2.2.2 # Check for the latest version
  uuid: ^4.3.3             # Check for the latest version
  intl: ^0.19.0
  google_maps_webservice: ^0.0.19
  flutter_dotenv: ^5.2.1
  # flutter_local_notifications: ^16.0.0 # Optional, for more advanced notifications

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/logo_light.png # Create a simple logo
    - assets/images/logo_dark.png  # Create a simple logo for dark mode
    - .env
  #   - images/a_dot_ham.jpeg
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700